<h1>Kittens!</h1>

<h2>Kittens that need some petting...</h2>

<ul>
    {{#each kittens}}
    {{#unless pet}}
    <li>
        {{name}}
        <button class="change-pet" data-id="{{id}}">Grumpy</button>
    </li>
    {{/unless}}
    {{/each}}
</ul>

<h2>Happy petted kittens!</h2>

<ul>
    {{#each kittens}}
    {{#if pet}}
    <li> {{name}} </li>
    {{/if}}
    {{/each}}
</ul>

<h2>Add a Kitten</h2>
<form class="create-form">

    <div class="form-group">
        <label for="ki">Kitten Name:</label>
        <input type="text" id="ki" name="name">
    </div>

    <button type="submit">Add Kitten</button>
</form>